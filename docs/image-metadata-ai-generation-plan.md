# 이미지 메타데이터 AI 생성 기능 개선 계획

## 📋 개요

골프 특화 이미지와 일반 이미지를 구분하여 각각에 최적화된 메타데이터를 생성하는 기능입니다.

---

## 🎯 목표

1. **골프 이미지**: 골프 특화 메타데이터 생성 (연령대 분석, 골프 카테고리 자동 결정 등)
2. **일반 이미지**: 범용 메타데이터 생성 (골프 제약 없이 모든 이미지 타입 지원)

---

## 📊 현재 상태

### ✅ 구현 완료
- `components/ImageMetadataModal/hooks/useAIGeneration.ts` - 기본 AI 생성 Hook
- `components/ImageMetadataModal/index.tsx` - 메타데이터 모달 UI
- `pages/api/analyze-image-prompt.js` - 골프 특화 이미지 분석 API
- `pages/api/admin/image-ai-analyzer.js` - 골프 특화 키워드 추출 API

### ⚠️ 구현 미완료 (계획 단계)
- `pages/api/analyze-image-general.js` - 범용 이미지 분석 API (신규 필요)
- `pages/api/admin/image-ai-analyzer-general.js` - 범용 키워드 추출 API (신규 필요)
- `useAIGeneration.ts`에 `generateGolfMetadata()`, `generateGeneralMetadata()` 함수 추가 필요
- `ImageMetadataModal/index.tsx`에 "골프 AI 생성", "일반 메타 생성" 버튼 추가 필요

---

## 🔧 구현 계획

### Phase 1: 범용 API 생성

#### 1-1. `/api/analyze-image-general.js` (신규)

**목적**: 골프 제약 없는 범용 이미지 분석

**기능**:
- 모든 이미지 타입 인식 (건물, 음식, 사람, 풍경, 제품 등)
- 골프 특화 프롬프트 제거
- 범용 프롬프트 사용

**프롬프트 예시**:
```
You are an expert image analyzer for general content.
Analyze the given image and create a detailed description.

Guidelines:
- Write in Korean language
- Focus on visual elements: composition, lighting, colors, objects, people, setting
- Include specific details if present
- Use descriptive adjectives and natural expressions
- Keep descriptions between 50-100 words
- Make it suitable for SEO and accessibility
```

**성능 최적화**:
- `max_tokens`: 300
- `temperature`: 0.3

---

#### 1-2. `/api/admin/image-ai-analyzer-general.js` (신규)

**목적**: 골프 특화 없이 일반 키워드 추출

**기능**:
- 모든 주제 키워드 추출 (건물, 음식, 사람, 풍경, 제품 등)
- 골프 특화 키워드 필터링 제거
- 범용 키워드 추출

**성능 최적화**:
- `max_tokens`: 100
- `temperature`: 0.1

---

### Phase 2: Hook 함수 개선

#### 2-1. `useAIGeneration.ts` 개선

**추가 함수**:
1. `generateGolfMetadata()` - 골프 메타데이터 생성 (기존 `generateAllMetadata` 리네임)
2. `generateGeneralMetadata()` - 범용 메타데이터 생성 (신규)

**차이점**:

| 항목 | 골프 모드 | 범용 모드 |
|------|----------|----------|
| API 엔드포인트 | `/api/analyze-image-prompt` | `/api/analyze-image-general` |
| 응답 형식 | JSON (alt_text, title, description, keywords, age_estimation) | JSON (alt_text, title, description, keywords) |
| 연령대 분석 | ✅ 포함 | ❌ 제거 (골프 특화) |
| 카테고리 자동 결정 | ✅ 포함 | ❌ 제거 |
| 카테고리 키워드 추가 | ✅ 포함 | ❌ 제거 |
| 제목 기본값 | "골프 이미지" | "이미지" |
| API 호출 수 | **1개** | **1개** |

---

### Phase 3: UI 개선

#### 3-1. `ImageMetadataModal/index.tsx` 버튼 추가

**변경 사항**:
- "한글 AI 생성" → "⛳ 골프 AI 생성" (텍스트 변경)
- "🌐 일반 메타 생성" 버튼 추가

**버튼 배치**:
```
[⛳ 골프 AI 생성] [🌐 일반 메타 생성]
```

---

## 📈 성능 비교

### 골프 모드
- **API 호출 수**: 1개 (단일 호출로 최적화)
- **예상 시간**: 2-3초
- **토큰 사용량**: ~800 tokens (max_tokens: 800)
- **특화 기능**: 연령대 분석, 골프 카테고리 자동 결정
- **응답 형식**: JSON (모든 메타데이터 한 번에 생성)
- **프롬프트 개선** (2025-11-09):
  - ALT text: 80-150 words (상세하고 생생한 설명)
  - Description: 100-200 words (풍부하고 맥락이 있는 설명)
  - Keywords: 8-12개 (골프 관련 키워드)

### 범용 모드
- **API 호출 수**: 1개 (단일 호출로 최적화)
- **예상 시간**: 2-3초
- **토큰 사용량**: ~800 tokens (max_tokens: 800)
- **특화 기능**: 없음 (범용 처리)
- **응답 형식**: JSON (모든 메타데이터 한 번에 생성)
- **프롬프트 개선** (2025-11-09):
  - ALT text: 80-150 words (상세하고 생생한 설명)
  - Description: 100-200 words (풍부하고 맥락이 있는 설명)
  - Keywords: 8-12개 (관련 키워드)

---

## 🔄 사용 방법

### 1. 골프 이미지
1. 이미지 메타데이터 모달 열기
2. "⛳ 골프 AI 생성" 버튼 클릭
3. 골프 중심 메타데이터 자동 생성
   - 연령대 분석 포함
   - 골프 카테고리 자동 결정
   - 골프 키워드 자동 추가

### 2. 일반 이미지
1. 이미지 메타데이터 모달 열기
2. "🌐 일반 메타 생성" 버튼 클릭
3. 범용 메타데이터 자동 생성
   - 골프 제약 없음
   - 모든 이미지 타입 지원
   - 빠른 처리 속도

---

## 📝 변경 파일 목록

### 신규 파일
- `pages/api/analyze-image-general.js`
- `pages/api/admin/image-ai-analyzer-general.js`
- `docs/image-metadata-ai-generation-plan.md` (이 문서)

### 수정 파일
- `components/ImageMetadataModal/hooks/useAIGeneration.ts`
  - `generateGolfMetadata()` 함수 추가 (기존 `generateAllMetadata` 리네임)
  - `generateGeneralMetadata()` 함수 추가 (신규)
- `components/ImageMetadataModal/index.tsx`
  - "골프 AI 생성" 버튼 추가
  - "일반 메타 생성" 버튼 추가
- `docs/project_plan.md`
  - 골프 AI 생성 기능 상태 업데이트

---

## ⚠️ 주의 사항

1. **하위 호환성**: 기존 `generateAllMetadata()` 함수는 `generateGolfMetadata()`로 리네임하되, 기존 호출 코드와의 호환성을 유지해야 함
2. **에러 핸들링**: 각 API 호출은 독립적으로 에러 처리되어야 함
3. **부분 성공 처리**: 일부 필드만 성공해도 결과를 반환해야 함

---

## 🚀 다음 단계

1. ✅ 문서 생성 완료 (이 문서)
2. ⏳ 범용 API 파일 생성 (`analyze-image-general.js`, `image-ai-analyzer-general.js`)
3. ⏳ Hook 함수 개선 (`useAIGeneration.ts`)
4. ⏳ UI 개선 (`ImageMetadataModal/index.tsx`)
5. ⏳ 테스트 및 검증

---

## 📚 참고 문서

- `docs/project_plan.md` - 전체 프로젝트 계획
- `components/ImageMetadataModal/hooks/useAIGeneration.ts` - 현재 구현 상태
- `pages/api/analyze-image-prompt.js` - 골프 특화 API 참고

---

**작성일**: 2025-11-09  
**상태**: 계획 단계 (구현 미완료)

