# 콘텐츠 및 이미지 프롬프트 자동 생성 가이드

## 📋 개요

월별, 주별, 일별 구조에 맞춰 허브 콘텐츠와 이미지 프롬프트를 자동으로 생성하는 스크립트입니다.

## 🚀 사용 방법

### 1. 환경 변수 설정

`.env.local` 파일에 OpenAI API 키가 설정되어 있어야 합니다:

```bash
OPENAI_API_KEY=your-api-key-here
```

### 2. 스크립트 실행

```bash
# 11월 콘텐츠 생성
node scripts/generate-monthly-content.js 2025-11

# 다른 월 생성 (예: 12월)
node scripts/generate-monthly-content.js 2025-12
```

## 📊 생성되는 콘텐츠

### 1. 허브 콘텐츠 (일별)

월별 전략과 주별 테마를 기반으로 일별 허브 콘텐츠를 생성합니다:

```json
{
  "date": "2025-11-12",
  "title": "제목 (60자 이내)",
  "summary": "요약 (150-200자, SMS/네이버 블로그용)",
  "overview": "간단한 개요 (300-400자)",
  "type": "신제품 소개",
  "channels": ["blog", "sms", "kakao", "naver_blog"],
  "status": "planned",
  "created": false
}
```

**생성 로직**:
- 월별 전략의 `contentTypes` 가중치에 따라 콘텐츠 유형 선택
- 주별 테마를 반영한 콘텐츠 생성
- 날짜별로 고유한 콘텐츠 생성

### 2. 이미지 프롬프트 개선

기존 프롬프트를 주별 테마와 계정별 톤에 맞게 개선합니다:

**프로필 콘텐츠**:
- 배경 이미지 프롬프트
- 프로필 이미지 프롬프트

**피드 콘텐츠**:
- 피드 이미지 프롬프트

**개선 기준**:
- 계정 1 (시니어): 따뜻한 골드·브라운 톤, 감성적 분위기
- 계정 2 (하이테크): 쿨 블루·그레이 톤, 현대적 분위기
- 주별 테마 반영
- 구체적이고 시각적으로 명확한 설명

## 📅 생성 일정

### 기본 설정
- **생성 기간**: 11월 11일 ~ 30일 (20일치)
- **생성 시간**: 약 5-10분 (API 호출 제한 고려)

### 콘텐츠 유형 분배

월별 전략의 `contentTypes` 가중치에 따라 자동 분배:

```json
{
  "신제품 소개": 5,
  "고객 후기": 4,
  "테스트 결과": 3,
  "시의성": 4,
  "이벤트": 3,
  "교육/팁": 2
}
```

## 🔄 워크플로우

1. **캘린더 파일 로드**
   - `docs/content-calendar/2025-11.json` 읽기

2. **허브 콘텐츠 생성** (일별)
   - 월별 전략 적용
   - 주별 테마 적용
   - 콘텐츠 유형 가중치 기반 선택
   - AI로 제목, 요약, 개요 생성

3. **이미지 프롬프트 개선**
   - 프로필 콘텐츠 프롬프트 개선
   - 피드 콘텐츠 프롬프트 개선
   - 계정별 톤 반영

4. **파일 저장**
   - 업데이트된 캘린더 파일 저장

## ⚙️ 설정 옵션

### 콘텐츠 생성 옵션

스크립트 내에서 수정 가능:

```javascript
// API 호출 간 대기 시간 (밀리초)
await new Promise(resolve => setTimeout(resolve, 2000)); // 허브 콘텐츠
await new Promise(resolve => setTimeout(resolve, 1000)); // 프롬프트 개선

// 생성 기간 조정
for (let day = 11; day <= 30; day++) { // 시작일, 종료일 변경 가능
```

### 프롬프트 커스터마이징

`generateHubContent` 함수의 프롬프트를 수정하여 생성되는 콘텐츠 스타일을 조정할 수 있습니다.

## 📝 생성 결과 확인

생성 완료 후 `docs/content-calendar/2025-11.json` 파일을 확인:

```bash
# 생성된 콘텐츠 개수 확인
cat docs/content-calendar/2025-11.json | jq '.contents | length'

# 특정 날짜 콘텐츠 확인
cat docs/content-calendar/2025-11.json | jq '.contents[] | select(.date == "2025-11-12")'
```

## ⚠️ 주의사항

1. **API 비용**: OpenAI API 호출 비용이 발생합니다
2. **생성 시간**: 20일치 콘텐츠 생성 시 약 5-10분 소요
3. **기존 콘텐츠**: 이미 생성된 콘텐츠(`created: true`)는 건너뜁니다
4. **프롬프트 개선**: 기존 프롬프트가 `enhanced: true`로 표시되면 건너뜁니다

## 🔧 문제 해결

### API 키 오류
```
Error: API key not found
```
→ `.env.local` 파일에 `OPENAI_API_KEY` 설정 확인

### JSON 파싱 오류
```
SyntaxError: Unexpected token
```
→ AI 응답이 JSON 형식이 아닐 수 있음. 프롬프트 확인 필요

### 파일 저장 오류
```
Error: EACCES: permission denied
```
→ 파일 권한 확인 또는 `sudo` 사용

## 📚 관련 문서

- [콘텐츠 캘린더 시스템](./README.md)
- [프로필 운영 가이드](./PROFILE_OPERATION_GUIDE.md)
- [작성 계획](./WRITING_PLAN.md)


