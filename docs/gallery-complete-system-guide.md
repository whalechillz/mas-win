# 갤러리 관리 시스템 완전 가이드

## 📋 개요

MASGOLF 갤러리 관리 시스템은 멀티 채널 콘텐츠 생산을 위한 핵심 인프라입니다. 블로그 이미지부터 제품 이미지, 고객 콘텐츠까지 모든 이미지 자산을 체계적으로 관리합니다.

---

## 🏗️ 시스템 아키텍처

### 핵심 원칙

1. **원본 보존 원칙 (Single Source of Truth)**
   - 모든 이미지는 `originals/` 폴더에 한 곳에만 물리적으로 존재
   - 중복 제거, 일관성 유지, 저장 공간 절약

2. **참조 기반 재사용 원칙**
   - 여러 곳에서 사용해도 원본은 복사하지 않고 메타데이터로 참조
   - 파일 복사 없음, 일관성 유지, 업데이트 용이

3. **채널별 베리에이션 생성 원칙**
   - 원본은 절대 이동/삭제하지 않음
   - 베리에이션만 `variants/` 폴더에 생성
   - 메타데이터에 원본 경로 항상 저장

---

## 📁 Storage 구조

```
masgolf-images/
├── originals/                    # 원본 이미지 (물리적 파일)
│   ├── blog/                     # ✅ 블로그 이미지 (우선 정리 대상)
│   │   ├── 2025-01/               # 날짜별 폴더
│   │   └── 2025-02/
│   ├── products/                 # 🟡 제품 이미지 (후속 작업)
│   ├── locations/                # 🟡 매장 이미지 (후속 작업)
│   ├── customers/                # 🟡 고객 콘텐츠 (후속 작업)
│   └── ...
├── variants/                     # 채널별 최적화 버전
│   └── {image-uuid}/
│       ├── webp/                 # WebP 변환
│       └── jpg/                   # JPG 변환
└── references/                   # 참조 메타데이터
    └── blog/
```

---

## 🚀 Phase별 개발 진행 상황

### 🔴 Phase 1: 인프라 준비 및 DB 설계 (완료 ✅)

#### 완료된 작업
- ✅ Supabase Storage에 `masgolf-images` 버킷 생성
- ✅ 블로그 이미지용 기본 폴더 구조 생성
- ✅ `image_metadata` 테이블 확장
- ✅ 해시 기반 검색 최적화 인덱스 생성

#### 주요 스키마
```sql
ALTER TABLE image_metadata
ADD COLUMN IF NOT EXISTS original_path TEXT,
ADD COLUMN IF NOT EXISTS internal_id VARCHAR(255),
ADD COLUMN IF NOT EXISTS hash_md5 VARCHAR(32),
ADD COLUMN IF NOT EXISTS hash_sha256 VARCHAR(64),
ADD COLUMN IF NOT EXISTS usage_count INTEGER DEFAULT 0,
ADD COLUMN IF NOT EXISTS "references" JSONB DEFAULT '[]',
ADD COLUMN IF NOT EXISTS blog_posts INTEGER[],
ADD COLUMN IF NOT EXISTS variants JSONB DEFAULT '{}';
```

**문서**: `docs/gallery-database-schema-notes.md`

---

### 🔴 Phase 2: 블로그 이미지 분석 및 분류 (완료 ✅)

#### 완료된 작업
- ✅ 모든 블로그 글 분석
- ✅ 이미지 URL 수집
- ✅ Storage에서 실제 파일 찾기
- ✅ 중복 이미지 감지 (해시 기반)

**API**: `POST /api/admin/analyze-all-blog-images`

---

### 🔴 Phase 3: 블로그 이미지 마이그레이션 및 메타데이터 동기화 (부분 완료 ⚠️)

#### 완료된 작업
- ✅ 블로그 이미지 이동 (`originals/blog/YYYY-MM/` 구조)
- ✅ 메타데이터 자동 생성
- ✅ URL 업데이트 (`image_url` 필드)

#### 미완료 작업
- ⚠️ 블로그 글 `featured_image` URL 업데이트
- ⚠️ 블로그 글 `content` 내 이미지 URL 업데이트 (Markdown/HTML)
- ⚠️ URL 업데이트 후 검증

**API**: `POST /api/admin/migrate-blog-images`

**문서**: `docs/gallery-migration-checklist.md`

---

### 🔴 Phase 4: 중복 이미지 제거 (완료 ✅)

#### 완료된 작업
- ✅ 중복 이미지 그룹 확인 (해시 기반)
- ✅ 안전한 중복 제거 (블로그 연결 이미지 보존)
- ✅ 메타데이터 정리

**API**: `POST /api/admin/remove-duplicate-blog-images`

**문서**: `docs/gallery-migration-priority-plan.md`

---

### 🔴 Phase 5: 프론트엔드 개발 편의성 개선 (완료 ✅)

#### 완료된 작업

##### Day 9: 폴더 트리 네비게이션 구현
- ✅ 폴더 트리 컴포넌트 개발 (`components/gallery/FolderTree.tsx`)
- ✅ 폴더별 이미지 조회 API
- ✅ 갤러리 UI 통합 (폴더 트리 사이드바)

**구현 파일**: `components/gallery/FolderTree.tsx`

**주요 기능**:
- 폴더 트리 렌더링
- 폴더 확장/축소
- 폴더 클릭 시 이미지 필터링
- 드래그 앤 드롭 지원

##### Day 10: 이미지 검색 및 필터링 강화
- ✅ 통합 검색 기능 (경로, 파일명, 태그/키워드, 블로그 글 제목)
- ✅ 필터 개선 (블로그 글별, 날짜별, 메타데이터 품질별, 사용 횟수별)
- ✅ 해시 기반 검색 (파일명 변경되어도 찾기)

**API**: `GET /api/admin/search-images?query={query}&filters={filters}`

##### Day 11: 이미지 카드 정보 확장 및 사용 편의성 개선
- ✅ 이미지 카드 정보 확장 (실제 폴더 경로, 연결된 블로그 글, 사용 횟수, 메타데이터 품질 배지)
- ✅ 빠른 작업 기능 (이미지 편집, 블로그 글 연결/해제, 폴더 이동, 베리에이션 생성)
- ✅ 일괄 작업 기능 (다중 선택, 일괄 메타데이터 수정, 일괄 폴더 이동, 일괄 삭제)

**문서**: `docs/gallery-migration-priority-plan.md`

---

### 🟡 Phase 6-7: 제품/고객 이미지 정리 (계획 단계 ⏳)

#### 계획된 작업
- ⏳ 제품 이미지 정리 (`originals/products/`)
- ⏳ 고객 콘텐츠 정리 (`originals/customers/`)
- ⏳ 매장 이미지 정리 (`originals/locations/`)

**문서**: `docs/gallery-architecture-principles.md`

---

### 🟡 Phase 8: 채널별 베리에이션 관리 (계획 단계 ⏳)

#### 계획된 작업
- ⏳ 채널별 베리에이션 생성 (SMS/MMS, Instagram, Facebook 등)
- ⏳ 베리에이션 자동 최적화 (크기, 포맷, 품질)
- ⏳ 베리에이션 메타데이터 관리

**문서**: `docs/gallery-architecture-principles.md`

---

## 🔧 주요 기능

### 1. 메타데이터 관리

#### 메타데이터 생성
- **골프 AI 생성**: 골프 특화 메타데이터 생성 (연령대 분석, 골프 카테고리 자동 결정)
- **일반 메타 생성**: 범용 메타데이터 생성 (골프 제약 없음) - **계획 단계**

**API**:
- `POST /api/analyze-image-prompt` - 골프 특화 이미지 분석
- `POST /api/admin/image-ai-analyzer` - 골프 특화 키워드 추출
- `POST /api/analyze-image-general` - 범용 이미지 분석 (계획)
- `POST /api/admin/image-ai-analyzer-general` - 범용 키워드 추출 (계획)

**문서**: `docs/image-metadata-ai-generation-plan.md`

#### 메타데이터 품질 검증
- ✅ 메타데이터 품질 점수 계산 (0-100점)
- ✅ 품질 이슈 목록 생성
- ✅ 갤러리 UI에 품질 정보 표시

**구현 파일**: `pages/api/admin/all-images.js`

---

### 2. EXIF 메타정보 추출

#### 완료된 기능
- ✅ EXIF 메타데이터 추출 (GPS 좌표, 촬영 날짜, 카메라 정보 등)
- ✅ 이미지 크기 정보 추출 (width, height)
- ✅ 일괄 EXIF 추출 및 메타데이터 동기화

**API**: `POST /api/admin/extract-exif`

**구현 파일**: `pages/api/admin/extract-exif.js`, `pages/api/admin/backfill-exif.js`

---

### 3. 구글 지도 연결

#### 현재 상태
- ✅ Contact 페이지에 구글 지도 임베드 구현
- ⚠️ 갤러리 시스템과 직접 연결 없음 (별도 기능)

**구현 파일**: `pages/contact.tsx`

**문서**: `docs/project_plan.md`

---

### 4. 트리 UI 사이드바

#### 완료된 기능
- ✅ 폴더 트리 렌더링
- ✅ 폴더 확장/축소
- ✅ 폴더 클릭 시 이미지 필터링
- ✅ 드래그 앤 드롭 지원

**구현 파일**: `components/gallery/FolderTree.tsx`

**주요 기능**:
```typescript
interface FolderTreeProps {
  folders: string[];
  selectedFolder: string;
  onFolderSelect: (folderPath: string) => void;
  includeChildren: boolean;
  onIncludeChildrenChange: (include: boolean) => void;
  onImageDrop?: (imageData, targetFolder: string) => void;
}
```

---

### 5. 중복 이미지 제거

#### 완료된 기능
- ✅ 해시 기반 중복 감지 (MD5, SHA256)
- ✅ 중복 이미지 그룹 표시
- ✅ 안전한 중복 제거 (블로그 연결 이미지 보존)

**API**: `POST /api/admin/remove-duplicate-blog-images`

**구현 파일**: `pages/api/admin/find-duplicates.js`, `pages/api/admin/remove-duplicates.js`

---

### 6. 블로그 이미지 정리

#### 완료된 기능
- ✅ 블로그 이미지 분석 및 분류
- ✅ 블로그 이미지 마이그레이션 (`originals/blog/YYYY-MM/` 구조)
- ✅ 메타데이터 동기화 및 AI 생성

**API**: `POST /api/admin/organize-images-by-blog`

**구현 파일**: `pages/api/admin/organize-images-by-blog.js`

---

## 📊 성능 최적화

### 구현된 최적화
- ✅ 캐싱 시스템 도입 (이미지 목록과 전체 개수 메모리 캐시)
- ✅ 일괄 메타데이터 조회 (N+1 쿼리 문제 해결)
- ✅ 재귀 폴더 조회 최적화 (불필요한 중복 조회 제거)
- ✅ 지연 로딩 (Intersection Observer 사용)
- ✅ 페이지당 이미지 수 감소 (30개 → 20개)
- ✅ 스크롤 이벤트 최적화 (디바운싱, passive 이벤트 리스너)

**문서**: `docs/gallery-performance-optimization.md`

---

## 📚 관련 문서

### 핵심 문서
1. `docs/gallery-architecture-principles.md` - 아키텍처 원칙 및 Storage 구조
2. `docs/gallery-database-schema-notes.md` - 데이터베이스 스키마 상세
3. `docs/gallery-migration-priority-plan.md` - Phase별 개발 계획
4. `docs/gallery-migration-checklist.md` - 마이그레이션 체크리스트
5. `docs/gallery-performance-optimization.md` - 성능 최적화 가이드

### 기능별 문서
- `docs/image-metadata-ai-generation-plan.md` - 메타데이터 AI 생성 계획
- `docs/image-metadata-review.md` - 메타데이터 시스템 개선 검토

### 데이터베이스
- `database/gallery-storage-schema.sql` - 데이터베이스 스키마 SQL

---

## 🔄 다음 단계

### 즉시 진행 가능
1. ⏳ 블로그 글 URL 업데이트 완료 (Phase 3 미완료 작업)
2. ⏳ 골프 AI 생성 vs 일반 메타 생성 기능 구현
3. ⏳ 제품/고객 이미지 정리 (Phase 6-7)

### 장기 계획
1. ⏳ 채널별 베리에이션 관리 (Phase 8)
2. ⏳ 추가 성능 최적화 (가상 스크롤링, 서비스 워커 캐싱)

---

## 📝 구현 파일 목록

### API 파일
- `pages/api/admin/all-images.js` - 모든 이미지 조회 및 메타데이터 품질 검증
- `pages/api/admin/analyze-all-blog-images.js` - 블로그 이미지 분석
- `pages/api/admin/organize-images-by-blog.js` - 블로그 이미지 정리
- `pages/api/admin/extract-exif.js` - EXIF 메타데이터 추출
- `pages/api/admin/backfill-exif.js` - 일괄 EXIF 추출
- `pages/api/admin/find-duplicates.js` - 중복 이미지 찾기
- `pages/api/admin/remove-duplicates.js` - 중복 이미지 제거
- `pages/api/admin/image-ai-analyzer.js` - 골프 특화 키워드 추출
- `pages/api/analyze-image-prompt.js` - 골프 특화 이미지 분석

### 컴포넌트 파일
- `components/gallery/FolderTree.tsx` - 폴더 트리 사이드바
- `components/ImageMetadataModal/index.tsx` - 메타데이터 모달 UI
- `components/ImageMetadataModal/hooks/useAIGeneration.ts` - AI 생성 Hook

### 페이지 파일
- `pages/admin/gallery.tsx` - 갤러리 관리 페이지
- `pages/contact.tsx` - Contact 페이지 (구글 지도 포함)

---

## ⚠️ 주의 사항

1. **`references` 컬럼 사용**: PostgreSQL 예약어이므로 항상 따옴표(`"references"`)로 감싸서 사용
2. **원본 보존**: `originals/` 폴더의 이미지는 절대 이동/삭제하지 않음
3. **메타데이터 동기화**: 이미지 이동 시 모든 참조 업데이트 필수
4. **안전한 중복 제거**: 블로그 연결 이미지는 절대 삭제하지 않음

---

**작성일**: 2025-11-09  
**최종 업데이트**: 2025-11-09  
**상태**: 진행 중 (Phase 1-5 완료, Phase 6-8 계획 단계)

