# 장면별 상세 UI 개선 계획

## 요구사항 분석

### 1. 미할당 이미지 드래그 드롭시 "장면1"로 돌아가는 현상 수정
**문제**: 미할당 이미지를 드래그 앤 드롭했을 때 의도하지 않게 "장면1"로 할당되거나 돌아가는 현상

**원인 분석**:
- 드롭 이벤트 처리 시 `targetScene`이 잘못 전달되거나 기본값으로 1이 설정되는 경우
- 드롭 대상 영역이 여러 개 겹쳐있어서 잘못된 영역에 드롭되는 경우
- API 호출 시 `storyScene` 값이 잘못 전달되는 경우

**해결 방안**:
- 드롭 이벤트에서 `targetScene` 값 명확히 확인
- 미할당 영역으로 드롭 시 `storyScene: null` 명시적으로 전달
- 드롭 대상 영역의 이벤트 버블링 방지 (`e.stopPropagation()`)

### 2. 장면 하단 이미지 -> 좌측 장면 목록의 특정 장면으로 이동 가능하게
**요구사항**: 장면 하단에 할당된 이미지를 드래그하여 좌측 장면 목록의 특정 장면 버튼으로 직접 드롭하여 이동 가능

**현재 상태**: 
- 장면 하단에서 미할당 영역으로 이동은 이미 가능함
- 장면 하단의 이미지를 다른 장면의 하단 영역으로 드래그하여 이동 가능
- **부족한 기능**: 좌측 장면 목록의 특정 장면 버튼으로 직접 드래그 앤 드롭 불가능

**해결 방안**:
- 좌측 장면 목록의 각 장면 버튼에 드롭 핸들러 추가
- 장면 버튼에 `onDragOver`, `onDrop` 이벤트 핸들러 설정
- 드롭 시 해당 장면 번호로 `storyScene` 업데이트
- 드래그 오버 시 시각적 피드백 제공 (버튼 하이라이트)

### 3. 미할당 미디어 섹션을 "장면3:문제발생" 제목 위로(맨 위) 이동
**요구사항**: 미할당 미디어 섹션을 장면 제목 위로 이동하여 더 자연스러운 UI 구성

**현재 구조**:
```
장면 3: 문제 발생 (제목)
├── 미할당 미디어 (10개) ← 현재 위치
└── 사진 (2) 탭
```

**제안 구조**:
```
미할당 미디어 (10개) ← 이동
장면 3: 문제 발생 (제목)
└── 사진 (2) 탭
```

**해결 방안**:
- 미할당 미디어 섹션을 장면 헤더 위로 이동
- 모든 장면에서 공통으로 표시되도록 구조 변경

### 4. 목록보기 필터 위치 및 탭 구성 개선
**요구사항**: 
- 옵션 1: 목록보기의 필터(전체, 이미지, 동영상)를 미할당 미디어 섹션 위로 이동
- 옵션 2: 탭을 4개로 구성 (미할당이미지, 이미지, 동영상, 서류, 전체)

**현재 구조**:
- 목록보기 탭 내부에 필터 버튼 존재
- 미할당 미디어 섹션과 분리되어 있음

**제안 구조 (옵션 1)**:
```
필터: [전체] [이미지] [동영상] [서류] ← 미할당 미디어 위로 이동
미할당 미디어 (10개)
장면 3: 문제 발생
└── 사진 (2) 탭
```

**제안 구조 (옵션 2)**:
```
탭: [미할당이미지] [이미지] [동영상] [서류] [전체]
├── 미할당이미지 탭: 미할당 이미지만 표시
├── 이미지 탭: 모든 이미지 표시
├── 동영상 탭: 모든 동영상 표시
├── 서류 탭: 모든 서류 표시
└── 전체 탭: 모든 미디어 표시
```

**권장 사항**: 옵션 1 (필터를 미할당 미디어 위로 이동)이 더 직관적이고 기존 구조와 일관성 유지

## 구현 계획

### Phase 1: 드래그 앤 드롭 버그 수정 및 기능 추가

**파일**: `components/admin/customers/SceneDetailView.tsx`

**수정 내용**:

1. **미할당 이미지 드롭 시 장면1로 돌아가는 문제 수정**
   ```typescript
   const handleDrop = async (e: React.DragEvent, targetScene: number | null) => {
     e.preventDefault();
     e.stopPropagation(); // 이벤트 버블링 방지
     
     // targetScene이 null이 아닌 경우에만 장면 할당
     // null인 경우는 미할당 영역으로 이동
     console.log('🔍 [드롭] targetScene:', targetScene);
     
     // ... 기존 로직
     
     // API 호출 시 storyScene 명시적으로 전달
     body: JSON.stringify({
       imageId,
       imageUrl,
       storyScene: targetScene === null ? null : targetScene // 명시적으로 null 처리
     })
   };
   ```

2. **미할당 영역 드롭 핸들러 확인**
   ```typescript
   <div
     onDragOver={(e) => {
       e.preventDefault();
       e.stopPropagation(); // 중요: 이벤트 버블링 방지
       handleDragOver(e, null);
     }}
     onDragLeave={handleDragLeave}
     onDrop={(e) => {
       e.preventDefault();
       e.stopPropagation(); // 중요: 이벤트 버블링 방지
       handleDrop(e, null); // 명시적으로 null 전달
     }}
   >
   ```

3. **장면 이미지에서 미할당으로 이동 기능 추가**
   - 장면 이미지의 드래그 핸들러는 이미 구현되어 있음
   - 미할당 영역의 드롭 핸들러만 확인하면 됨

### Phase 2: UI 구조 재배치

**파일**: `components/admin/customers/SceneDetailView.tsx`

**수정 내용**:

1. **미할당 미디어 섹션을 맨 위로 이동**
   ```typescript
   return (
     <div className="grid grid-cols-3 gap-4">
       {/* 왼쪽: 장면 목록 */}
       <div className="col-span-1 space-y-2">
         {/* 장면 목록 */}
       </div>

       {/* 오른쪽: 선택된 장면 상세 */}
       <div className="col-span-2">
         <div className="bg-white border border-gray-200 rounded-lg p-4">
           {/* 미할당 미디어 섹션 - 맨 위로 이동 */}
           {unassignedMedia.length > 0 && (
             <div className="mb-6 border-2 border-dashed border-blue-300 rounded-lg p-4 bg-blue-50">
               {/* 미할당 미디어 내용 */}
             </div>
           )}

           {/* 장면 헤더 */}
           <div className="mb-4">
             <h3 className="text-lg font-semibold text-gray-900">
               장면 {activeScene}: {SCENE_NAMES[activeScene]}
             </h3>
           </div>

           {/* 탭 메뉴 및 내용 */}
         </div>
       </div>
     </div>
   );
   ```

### Phase 3: 필터 위치 개선

**파일**: `components/admin/customers/SceneDetailView.tsx`

**옵션 1 구현 (권장)**:

1. **필터를 미할당 미디어 위로 이동**
   ```typescript
   return (
     <div className="grid grid-cols-3 gap-4">
       {/* 왼쪽: 장면 목록 */}
       <div className="col-span-1 space-y-2">
         {/* 장면 목록 */}
       </div>

       {/* 오른쪽: 선택된 장면 상세 */}
       <div className="col-span-2">
         <div className="bg-white border border-gray-200 rounded-lg p-4">
           {/* 필터 - 맨 위 */}
           <div className="mb-4 flex gap-2">
             <button
               onClick={() => setMediaTypeFilter('all')}
               className={`px-3 py-1 text-sm rounded ${
                 mediaTypeFilter === 'all'
                   ? 'bg-blue-600 text-white'
                   : 'bg-gray-200 text-gray-700 hover:bg-gray-300'
               }`}
             >
               전체
             </button>
             <button onClick={() => setMediaTypeFilter('image')}>이미지</button>
             <button onClick={() => setMediaTypeFilter('video')}>동영상</button>
             <button onClick={() => setMediaTypeFilter('document')}>서류</button>
           </div>

           {/* 미할당 미디어 섹션 - 필터 아래 */}
           {unassignedMedia.length > 0 && (
             <div className="mb-6 border-2 border-dashed border-blue-300 rounded-lg p-4 bg-blue-50">
               {/* 미할당 미디어 내용 */}
             </div>
           )}

           {/* 장면 헤더 및 탭 */}
         </div>
       </div>
     </div>
   );
   ```

2. **목록보기 탭의 필터 제거** (상단 필터로 통합)

**옵션 2 구현 (대안)**:

1. **탭을 5개로 구성**
   ```typescript
   const [activeTab, setActiveTab] = useState<'unassigned' | 'images' | 'videos' | 'documents' | 'all' | 'description' | 'list'>('images');
   
   // 탭 메뉴
   <nav className="flex space-x-4">
     <button onClick={() => setActiveTab('unassigned')}>미할당이미지</button>
     <button onClick={() => setActiveTab('images')}>이미지</button>
     <button onClick={() => setActiveTab('videos')}>동영상</button>
     <button onClick={() => setActiveTab('documents')}>서류</button>
     <button onClick={() => setActiveTab('all')}>전체</button>
     <button onClick={() => setActiveTab('description')}>장면 설명</button>
     <button onClick={() => setActiveTab('list')}>목록보기</button>
   </nav>
   ```

2. **각 탭에 해당하는 미디어만 표시**
   - 미할당이미지: `unassignedImages`
   - 이미지: 모든 이미지 (동영상, 서류 제외)
   - 동영상: 모든 동영상
   - 서류: 모든 서류
   - 전체: 모든 미디어

**권장 사항**: 옵션 1이 더 직관적이고 기존 구조와 일관성 유지

## 상세 구현 내용

### 1. 드래그 앤 드롭 버그 수정

**문제 1: 미할당 이미지가 장면1로 돌아가는 현상**

**원인**:
- 드롭 이벤트가 여러 요소에 전파되어 잘못된 `targetScene` 값이 전달됨
- 미할당 영역의 드롭 핸들러가 제대로 작동하지 않음

**해결책**:
```typescript
// 미할당 영역 드롭 핸들러
<div
  onDragOver={(e) => {
    e.preventDefault();
    e.stopPropagation(); // 중요!
    setDragOverUnassigned(true);
  }}
  onDragLeave={(e) => {
    e.preventDefault();
    e.stopPropagation(); // 중요!
    setDragOverUnassigned(false);
  }}
  onDrop={(e) => {
    e.preventDefault();
    e.stopPropagation(); // 중요!
    handleDrop(e, null); // 명시적으로 null 전달
  }}
>
```

**문제 2: 장면 이미지에서 좌측 장면 목록으로 이동**

**해결책**:
- 좌측 장면 목록의 각 장면 버튼에 드롭 핸들러 추가
- 장면 버튼에 `onDragOver`, `onDrop` 이벤트 핸들러 설정
- 드롭 시 해당 장면 번호로 `storyScene` 업데이트
- 드래그 오버 시 버튼 스타일 변경 (예: 배경색 변경, 테두리 강조)

### 2. UI 구조 재배치

**변경 전**:
```
장면 3: 문제 발생
├── 미할당 미디어 (10개)
└── 탭
    ├── 사진 (2)
    ├── 장면 설명
    └── 목록보기 (13)
```

**변경 후 (옵션 1)**:
```
필터: [전체] [이미지] [동영상] [서류]
미할당 미디어 (10개)
장면 3: 문제 발생
└── 탭
    ├── 사진 (2)
    ├── 장면 설명
    └── 목록보기 (13)
```

**변경 후 (옵션 2)**:
```
탭: [미할당이미지] [이미지] [동영상] [서류] [전체] [장면 설명] [목록보기]
└── 선택된 탭 내용
```

### 3. 필터 기능 개선

**옵션 1 (권장)**:
- 상단에 필터 버튼 배치
- 필터는 미할당 미디어와 목록보기 탭 모두에 적용
- 미할당 미디어 섹션도 필터링됨

**옵션 2**:
- 탭으로 분리하여 각 타입별로 독립적인 뷰 제공
- 더 많은 탭으로 인한 UI 복잡도 증가

## 파일 구조

### 수정할 파일
1. `components/admin/customers/SceneDetailView.tsx`
   - 드래그 앤 드롭 버그 수정
   - UI 구조 재배치
   - 필터 위치 변경 또는 탭 구성 변경

### 참고 파일
1. `components/admin/CustomerStoryModal.tsx` - 드래그 앤 드롭 로직 참고
2. `pages/api/admin/update-image-scene.ts` - API 엔드포인트

## 예상 작업 시간

- Phase 1 (드래그 앤 드롭 버그 수정 및 좌측 장면 목록 드롭 기능 추가): 2-3시간
- Phase 2 (UI 구조 재배치): 1시간
- Phase 3 (필터 위치 개선): 1-2시간
- 테스트 및 디버깅: 1시간
- **총 예상 시간: 5-7시간**

## 우선순위

**높음**: 사용자가 직접 보고한 기능 오류 및 UI/UX 개선 요청

## 테스트 계획

1. **드래그 앤 드롭 테스트**
   - 미할당 이미지를 장면 하단 영역으로 드래그 → 장면에 할당되는지 확인
   - 미할당 이미지를 좌측 장면 목록의 특정 장면 버튼으로 드래그 → 해당 장면에 할당되는지 확인
   - 장면 하단 이미지를 미할당 영역으로 드래그 → 미할당으로 이동하는지 확인 (이미 가능)
   - 장면 하단 이미지를 좌측 장면 목록의 다른 장면 버튼으로 드래그 → 해당 장면으로 이동하는지 확인
   - 장면 하단 이미지를 좌측 장면 목록의 같은 장면 버튼으로 드래그 → 변화 없음 확인
   - 미할당 이미지를 미할당 영역으로 드래그 → 장면1로 돌아가지 않는지 확인

2. **UI 구조 테스트**
   - 미할당 미디어 섹션이 장면 제목 위에 표시되는지 확인
   - 필터가 미할당 미디어 위에 표시되는지 확인
   - 모든 장면에서 일관된 레이아웃인지 확인

3. **필터 기능 테스트**
   - 필터 버튼 클릭 시 미할당 미디어가 필터링되는지 확인
   - 목록보기 탭에서도 필터가 적용되는지 확인
   - 각 필터 타입별로 올바른 미디어만 표시되는지 확인

## 권장 사항

1. **옵션 1 (필터 상단 이동) 권장**
   - 기존 구조와 일관성 유지
   - UI 복잡도 증가 최소화
   - 사용자 학습 곡선 낮음

2. **단계별 구현**
   - Phase 1 (버그 수정) → Phase 2 (UI 재배치) → Phase 3 (필터 개선) 순서로 진행
   - 각 단계마다 테스트 후 다음 단계 진행

3. **사용자 피드백 반영**
   - 옵션 1과 옵션 2 중 선택은 사용자 피드백에 따라 결정
   - 필요시 하이브리드 방식도 고려 가능
